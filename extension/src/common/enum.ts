export enum ActionEnum {
    LOGIN = "login",
    FORM_INTERACTION = "form_interaction",
    SET_STATE = "set_state",
}

export enum StorageEnum {
    LOCAL = "local",
    SESSION = "session",
    INDEXEDDB = "index",
}
